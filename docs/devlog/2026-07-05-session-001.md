# SHAI Devlog - 2026-07-05 - Session 001

## Summary

Added a required devlog and patch notes workflow so future SHAI development sessions have a clear record of what changed, why it changed, and how to test it.

## Why

The project needs lightweight human-readable history for Codex and developer changes beyond Git commits. A devlog makes implementation context, verification steps, known issues, and follow-up work easier to review later.

## Files Changed

- `docs/devlog/README.md`: Explains the devlog policy and helper script.
- `docs/devlog/2026-07-05-session-001.md`: Records the current session.
- `scripts/new_devlog.py`: Adds a helper for creating dated, incrementing devlog entries.
- `CONTRIBUTING.md`: Requires devlog entries for meaningful code changes.
- `README.md`: Links to the devlog and documents the patch notes workflow.

## Features Added

- Added a `docs/devlog/` folder for session patch notes.
- Added a reusable devlog entry template.
- Added an auto-incrementing `scripts/new_devlog.py` helper.
- Added README and contributing guidance for the workflow.

## Bugs Fixed

- None.

## Tests Run

- `.\.venv\Scripts\python.exe -m py_compile scripts\new_devlog.py`: Passed.
- `.\.venv\Scripts\python.exe -m compileall spool_house_ai`: Passed.
- `.\.venv\Scripts\python.exe -m spool_house_ai.main --test`: Passed.

## Known Issues

- None known.

## Next Suggested Steps

- Use `python scripts/new_devlog.py` before the next meaningful project change.
- Keep devlog entries focused on real changes, test results, and follow-up work.
